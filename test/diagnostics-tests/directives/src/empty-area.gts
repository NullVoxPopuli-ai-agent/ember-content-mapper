// Each directive below covers a line that transforms to nothing.
<template>
  {{! @glint-expect-error: the next line is blank }}

  {{! @glint-expect-error: the next line closes the template }}
</template>
